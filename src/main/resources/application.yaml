spring:
  application:
    name: track
  datasource:
    url: jdbc:mysql://localhost:3307/track
    username: track
    password: track
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true