apiVersion: v1
kind: Service

metadata:
  name: track-app-service
  namespace: track

spec:
  selector:
    app: track-app

  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP